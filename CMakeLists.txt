
# Envelope - workaround third party bugs and features

cmake_minimum_required (VERSION 3.15.4)

include(./public/core/list.cmake)
enInitialization()
project(${enProjectName})
enImplemenation()
enEncapsulation()
enFinalization()